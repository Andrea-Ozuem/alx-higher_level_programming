#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None
    max_iem = my_list[0]
    for i in my_list:
        if max_item < i:
            max_item = i
    return max_item
